# Use exact versions for reproducible builds
save-exact=true

# Hoist all dependencies to root node_modules
hoist=true

# Engine strict mode
engine-strict=true

# Disable package-lock in workspaces (use root lock file)
workspaces-update=true
